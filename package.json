{
  "name": "white-zero-platform",
  "version": "1.0.0",
  "description": "White Zero Intelligence Platform - Forensic & OSINT Framework",
  "scripts": {
    "frontend:test": "npm test --prefix frontend",
    "frontend:report": "npm run test:report --prefix frontend",
    "test": "playwright test -c INTEGRITY_AUDIT/playwright.config.js",
    "test:report": "playwright show-report INTEGRITY_AUDIT/playwright-report",
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend"
  },
  "author": "White Zero Team",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.7.0"
  }
}
